# ============================================
# quebexico_co - Variables d'environnement
# ============================================
# Copier en .env et remplir (ou copier depuis quebexico les valeurs communes).

# --- Obligatoire pour démarrer le serveur ---
# Base de dev dans .env (recommandé). Pour tester en local avec les données de prod :
# 1. Créer .env.prod (gitignored) avec une seule ligne : DATABASE_URL=<url Neon prod>
# 2. Lancer : npm run dev:prod  (ou USE_PROD_DB=1 npm run dev)
# Par défaut (npm run dev) : utilise DATABASE_URL du .env = base de dev.
DATABASE_URL=

# --- Obligatoire si tu utilises l'admin (chiffrement des clés) ---
# Même valeur que dans le .env de quebexico, ou générer : openssl rand -base64 32
SESSION_SECRET=

# --- Admin (optionnel : défaut = admin-secret-key) ---
ADMIN_SECRET_KEY=

# --- Connexion quebexico.com (réservation directe) ---
# Sans ces variables, le module réservation ne charge pas de propriétés.
# Tu peux aussi les renseigner dans Admin → Intégrations.
DIRECT_SITE_API_URL=https://quebexico.com
DIRECT_SITE_API_KEY=

# --- Optionnel ---
# PORT=5000
# PEXELS_API_KEY=
# AI_INTEGRATIONS_OPENAI_API_KEY=
# AI_INTEGRATIONS_OPENAI_BASE_URL=
